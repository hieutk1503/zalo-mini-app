# Môi trường phát triển (dev) – DÙNG API THẬT (VITE_USE_MOCK=false), không dùng mock
VITE_BASE_URL=http://localhost:3001
VITE_MINI_APP_ID=dev-mini-app-id
VITE_USE_MOCK=false
# Token tĩnh CHỈ dùng cho dev (chạy ngoài app Zalo). Production để TRỐNG.
# Cần có giá trị để app nạp được dữ liệu khi chạy bằng trình duyệt.
VITE_DEV_TOKEN=dev-token-local

